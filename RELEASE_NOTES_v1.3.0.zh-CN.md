# stock_analysis v1.3.0 — 更新说明

## 主要更新

### 新功能
- 全新 Web 图形界面（Liquid Glass 风格）作为默认启动界面，含「分析 / 回测·优化 / 设置 / 帮助」四个标签页；旧 Tkinter 界面仍保留（`python -m src.app.gui`）。
- 新增「回测 / 参数优化」：MA 均线交叉策略回测（A 股交易成本模型、支持跨标的与滚动窗口）、参数网格优化（收益/夏普等目标）与「采用参数」联动分析页 MA 周期。
- 新增「帮助」标签页：内置七大部分使用说明（可调字号）、「检查更新」与一键下载并静默安装更新（仅官方发布源）。
- 结果预览支持 Markdown 渲染（默认）与纯文本双视图切换。
- API 设置新增可选字段：麦蕊 Licence、Biyingapi AppCode（供资金流交叉核验脚本使用）。
- 批量分析支持并发、分项进度与取消；LLM 报告在界面流式输出。
- 接入 CNINFO 官方公告事件作为证据维度（未知标题保持中性，不推断方向）。

### 修复与改进
- 修复深度分析报告被输出上限截断、正文为空的问题（深度模式使用独立上限，实测不再截断）。
- 修复回测/参数优化未规范化股票代码、导致主数据源不可用的问题（如 `002001` 可直接回测）。
- 修复安装版数据缓存不可用的问题（打包恢复 pyarrow）：同日重复分析命中本地缓存，降低成本。
- 改进结构化变化对比：修复 changes 键递归与 list/tuple 比较噪音。
- 提升 LLM 输出上限，避免长报告被截断。

## 验证情况
- 测试套件：276 项通过（另有 1 项公开接口联网测试因本机代理不可达未通过，与代码无关）。
- Web GUI 真实环境交互已验收：分析、回测/优化、帮助、检查更新、Markdown 预览。
- 深度分析实测（002001/600183）报告完整输出；回测裸代码正常；安装版缓存已生效。

## 分发文件
- `StockAnalysis-Setup-1.3.0.exe`
- `checksums.sha256`
- `release-manifest.json`
- `RELEASE_NOTES_v1.3.0.en.md`
- `RELEASE_NOTES_v1.3.0.zh-CN.md`
- `THIRD_PARTY_NOTICES.md`

## 使用说明
- 正常获取数据需配置 Tushare Token；LLM 配置为可选项（深度分析建议配置深度模型）。
- 升级安装会保留 `%LOCALAPPDATA%\StockAnalysis\` 下的现有设置与用户数据。
- 本工具不执行交易，也不连接券商账户。
