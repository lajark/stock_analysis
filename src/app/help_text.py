"""Shared user-facing help text (usage guide).

Kept outside the GUI modules so both the legacy tkinter shell and the new web
GUI render the same Markdown guide from a single source of truth.
"""

USAGE_GUIDE_MD = """# 股票分析工具 使用说明

本工具是个人股票分析工具：**手动触发、本地计算为主**，LLM 仅用于生成中文报告（可选）。
所有行情、财务数据均来自公开数据源；工具不会执行真实交易。

---

## 一、首次使用：API 配置

第一次打开时请先进入 **「API 设置」** 标签页完成配置，然后点击 **「保存设置」**。
配置仅保存在当前 Windows 用户的本地目录，不会上传到任何服务器。

### 1. Tushare Token（必需）
- 用途：获取行情、财务等核心数据的主数据源。
- 获取：前往 [Tushare Pro](https://tushare.pro) 注册并登录 → 右上角「个人主页」→
  「接口TOKEN」→ 复制该 Token。
- 填写：在 **「API 设置」** 页第一行「Tushare Token」输入框粘贴并保存。
- 不配置则无法开始分析。

### 2. LLM 配置（可选，使用 AI 报告时需要）
| 字段 | 说明 |
|------|------|
| LLM API Key | 供应商的 API Key。默认阿里云百炼（DashScope），也兼容任何 OpenAI 兼容接口 |
| LLM 接口地址 | 默认 `https://dashscope.aliyuncs.com/compatible-mode/v1`，可下拉选择或自行填写 |
| 普通模型 | 默认 `deepseek-v4-flash`，用于日常分析 |
| 深度分析模型 | 默认 `deepseek-v4-pro`，用于深度分析/交易计划模式 |

- 不使用 AI 报告时无需填写 LLM 配置；分析时取消勾选「使用 AI 生成中文报告」即可零 Token 消耗。
- 每次分析默认只调用 1 次 LLM，完整原始数据不会发送给 LLM，只发送精简后的分析包（成本控制）。

### 3. 可选备用证书（供资金流交叉核验等研究脚本使用）
- **麦蕊 Licence**：用于拉取 2010 年至今的全历史资金流数据，可作为
  Tushare 资金流历史数据的免费核验 / 替代源。
- **Biyingapi AppCode**：备用行情数据接口证书。
- 这两项目前仅被 CLI 研究脚本使用，**不参与**主分析流程，可留空。

---

## 二、股票分析

### 1. 分析模式

| 模式 | 说明 |
|------|------|
| 快速扫描 | 快速分析，含核心指标和技术面 |
| 深度分析 | 全面分析，含基本面、估值、风险 |
| 交易决策 | 侧重交易策略和关键价位 |
| 价值评估 | 侧重估值分析和合理价位 |

### 2. 单只股票分析
1. 在「股票代码」框输入代码（如 `600519` 或 `600519.SH`）。
2. 选择分析模式。
3. 勾选是否使用 AI 生成报告、是否同时生成 K 线图。
4. 点击 **「开始分析」**。
5. 等待各阶段进度推进；若勾选 AI，报告会流式输出到预览区。
6. 完成后点击 **「打开结果文件」** 查看完整报告，或 **「打开输出目录」** 查看所有产物。

### 3. 批量分析
1. 输入多只股票代码，用逗号、空格或换行分隔（例如 `600519, 000858, 601318`）。
2. 点击 **「开始分析」**。批量模式自动关闭流式预览，只显示每只的摘要。
3. 可随时点击 **「取消分析」** 整批取消。

### 4. 取消操作
- 分析过程中可随时点击「取消分析」；取消后不生成部分报告，预览区可能保留已流出的文本。

### 5. 结果预览与 Markdown 渲染
- 分析完成后，预览区默认以 **Markdown 渲染** 方式显示报告（表格、标题、代码高亮）。
- 若同时勾选了 K 线图，预览区分为左右两栏：左侧报告、右侧图表。
- 工具栏可切换 **浅色 / 深色** 主题，并用 **A- / A+** 调整字号。

---

## 三、回测与参数优化

### 1. 功能说明
- 内置策略：**双均线交叉（MA 金叉/死叉）**。当日收盘价确定信号，**次日开盘价** 成交。
- 成本模型：包含佣金、印花税、过户费与滑点，按 100 股整手买入。
- 这是研究工具，**不会**下单、不连接券商，仅用于评估策略在历史行情上的表现。
- 策略是开放的：内置策略源码位于 `src/analysis/strategies.py`，你也可以在
  「设置 → 策略」中编辑**用户策略文件**（`<用户数据>/strategies/ma_cross.py`），
  修改信号规则或参数后立即生效；文件损坏时自动回退内置策略。

### 2. 单次回测（「回测 / 优化」标签页）
1. 输入股票代码（如 `600519.SH`）。
2. 输入起止日期（格式 `YYYY-MM-DD`，如 `2024-01-01` 到 `2026-01-01`）。
3. 设置快均线周期（如 20）与慢均线周期（如 60），要求快 < 慢。
4. 点击 **「运行回测」**。

**结果解读：**
| 指标 | 含义 |
|------|------|
| 总收益率 / 年化收益率 | 策略在该窗口的整体收益 |
| 基准收益 | 同期买入持有（Buy & Hold）的收益，用于对比 |
| 最大回撤 | 账户净值从高点回落的最大幅度 |
| 夏普 / 索提诺比率 | 风险调整后收益，越高越好 |
| 交易次数 / 胜率 | 完成的往返交易次数与盈利占比 |
| 持仓暴露 | 处于持仓状态的时间占比 |

### 3. 参数优化
1. 在「参数网格」中为每个策略参数填写要遍历的数值（逗号分隔）。
2. 选择优化目标（夏普比率 / 总收益 / Calmar / 稳健分）。
3. 点击 **「参数优化」**。程序会把数据按 训练/验证/测试 三段切分，
   只在训练段选参数，再用验证/测试段检验，避免过拟合。
4. 结果展示选中的参数组合与分段表现。

### 4. 优化参数如何用于分析（不自动采用）
- 优化结果默认**不会**改动任何分析逻辑。
- 若你同意，点击结果区的 **「采用建议参数用于分析」**：参数会保存到本地配置，
  分析页出现「使用策略建议参数」勾选项。
- 勾选后，技术指标中的 MA 周期会使用这些参数，并在报告中注明；取消勾选即恢复默认。
- 这些参数仅用于研究参考，**不会自动晋升**到生产分析。

### 5. 为什么回测结果是 0 或没有交易？
这是**策略在特定行情下的正常行为**，不是故障。交易次数为 0 有两种情况：

- **全程空仓**：所选窗口内快、慢均线从未发生金叉（例如整段单边下跌、或横盘震荡时
  均线长期纠缠不交叉），因此一直空仓，总收益率也就接近 0。
- **买入后持有至期末**：信号买入后行情持续向好、未再出现死叉，持仓一直延续到窗口
  结束，因此没有「完整往返交易」记录。此时期末持仓中反映的是未实现的浮盈/浮亏，
  总收益率可能并不为 0。
- 处置建议：
  - 把回测窗口**缩短**到包含明显趋势行情的区间；
  - 调整均线周期（如缩小到 5/20、10/30），更灵敏的均线更容易触发交叉；
  - 换一只在窗口内趋势更清晰的股票；
  - 用「参数优化」自动扫描多种参数组合。
- 工具会在「交易次数 = 0」时给出上述两种情况的具体提示，同时展示基准收益作为对照。

---

## 四、检查更新

- 在「帮助」页点击 **「检查更新」**，工具会通过 GitHub / Gitee 的 Release
  接口比对版本号。
- 发现新版本时，可点击 **「下载并安装更新」**：应用会下载官方安装包，然后**静默安装**
  （`/VERYSILENT`，安装器自动关闭正在运行的应用后再替换程序文件）。
- **更新不会覆盖你的用户数据**：安装目录为 `%LOCALAPPDATA%\\Programs\\StockAnalysis`，
  而你的设置、缓存、输出、策略等数据在 `%LOCALAPPDATA%\\StockAnalysis`，两者完全分离；
  更新/卸载都不会触碰后者。
- 更新检查为手动触发；网络不可用时显示失败信息，不影响本地使用。

---

## 五、常见问题

- **Q：数据没更新 / 提示数据获取失败？**
  工具会记录警告日志并自动降级到备用数据源（AkShare）。可在启动时加 `--debug`
  查看详细日志（日志位于用户数据目录 `logs/` 下）。
- **Q：基本面数据缺失 / 财务数据为空？**
  财务接口偶发失败时，工具会自动重试一次；若仍失败会在报告中如实标注"财务数据获取失败"。
  **规避**：稍后**重新分析一次**即可恢复（数据会写入缓存）；也可删除
  `%LOCALAPPDATA%\\StockAnalysis\\data\\cache\\financials` 下对应股票的缓存后重试。
- **Q：我的 Token / Key 安全吗？**
  设置仅保存在本机用户目录，界面默认以掩码显示，可点击「显示密钥」查看。
- **Q：同一只股票当天会重复请求外部接口吗？**
  不会。同一天同一股票的数据会被本地缓存，重复分析不会再次请求外部 API。
- **Q：回测/优化结果会被自动采用吗？**
  不会。回测与优化结果仅用于研究参考，参数需要你手动确认后才可能用于分析。

---

*更多技术细节见项目文档与 `--help`。本说明可在右上角切换浅/深色主题并调整字号阅读。*
"""
