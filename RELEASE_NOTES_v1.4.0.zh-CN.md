# stock_analysis v1.4.0 — 更新说明

## 主要更新

### 新功能
- 支持北交所股票代码（`.BJ`）：识别 4 / 8 / 920 代码段与显式 `.BJ` 后缀，主数据源 Tushare 与降级源 AkShare 均可取数；桌面版、Web 版与 CLI 可直接输入北交所代码（如 `430047`、`832566`、`920001`）。
- 各入口统一股票代码校验与规范化：桌面版分析/回测/优化、Web 版分析接口与 CLI 在派发前统一规范化代码（如 `002001 → 002001.SZ`、`430047 → 430047.BJ`），非法代码、空输入与「代码段与后缀不一致」（如 `600519.BJ`、`430047.SH`）在入口即被拦截并给出可读提示，不再进入取数流程。
- 长报告 Token 预算（extended）：value / deep 模式共享独立输出上限，长报告不再因输出上限被截断。
- 一键更新多源容错：安装包下载按 Gitee 优先、GitHub 兜底自动切换；当发布未附带安装包时，界面给出发布页链接供手动下载；修复安装版更新检测无法读取版本号的问题（打包内嵌 pyproject.toml）。

### 修复与改进
- 修复股票代码后缀错标时静默取数失败的问题：现在会明确提示正确的市场后缀。
- 修复安装版（PyInstaller 产物）无法读取自身版本号、导致「检查更新」判断异常的问题。
- 更新安装流程白名单收紧：下载源必须在官方白名单内，任一外来源整体拒绝。
- 内部重构：估值指标取整逻辑收敛、价格层级回撤估算清理、`Optional[x]` → `x | None` 等类型注解现代化、清理未使用导入与多余空行。

## 验证情况
- 离线测试套件：326 项通过（另有 1 项公开接口联网契约测试因本机代理不可达未通过，与代码无关，并按 CI 规则默认排除）。
- ruff（src/ 与 tests/ 全树）通过；mypy 维护集 0 问题。
- 北交所代码校验（`430047.BJ` / `832566` / `920001` 等）与各入口规范化已由单元测试覆盖。

## 分发文件
- `StockAnalysis-Setup-1.4.0.exe`
- `checksums.sha256`
- `release-manifest.json`
- `RELEASE_NOTES_v1.4.0.en.md`
- `RELEASE_NOTES_v1.4.0.zh-CN.md`
- `THIRD_PARTY_NOTICES.md`

## 使用说明
- 正常获取数据需配置 Tushare Token；LLM 配置为可选项（深度/价值分析建议配置深度模型）。
- 升级安装会保留 `%LOCALAPPDATA%\StockAnalysis\` 下的现有设置与用户数据。
- 本工具不执行交易，也不连接券商账户。
